# Configuration variables
# Replace placeholders with actual values

app:
  name: "Leaf Plate Sales"
  version: "1.0.0"
  description: "Web application for selling leaf plates"

database:
  url: "postgresql://user:password@hostname:port/dbname" # SUPABASE connection string
  key: "SUPABASE_KEY_PLACEHOLDER"

api:
  host: "0.0.0.0"
  port: 8000
  allow_origins:
    - "http://localhost:5173" # Frontend local dev URL
    - "http://localhost:5174"
    - "http://localhost:5175"
    - "https://webplate-delta.vercel.app" # Backend Vercel URL (for testing)
    - "https://webplate-nav.netlify.app/" # Replace with your Netlify URL after deploying frontend

contact:
  email: "naveenlenin934@gmail.com"
  phone: "+91 9080708039"
