# Configuration variables
# Replace placeholders with actual values

app:
  name: "Leaf Plate Sales"
  version: "1.0.0"
  description: "Web application for selling leaf plates"

database:
  # DATABASE_URL is loaded from backend/.env — do NOT store real credentials here
  url: "" # managed via DATABASE_URL env var
  key: "" # managed via SUPABASE_KEY env var if needed

api:
  host: "0.0.0.0"
  port: 8000
  allow_origins:
    - "http://localhost:5173" # Frontend local dev URL
    - "http://localhost:5174"
    - "http://localhost:5175"
    - "https://webplate-delta.vercel.app" # Backend Vercel URL (for testing)
    - "https://webplate-nav.netlify.app" # Netlify frontend URL

payment_gateway:
  url: "http://localhost:8001" # Local dev; change to deployed URL for production
  webhook_secret: "supersecret123change_me" # Must match WEBHOOK_SECRET in Payment_gateway

contact:
  email: "naveenlenin934@gmail.com"
  phone: "+91 9080708039"
